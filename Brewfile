# Brewfile - install dependencies with: brew bundle
# https://github.com/Homebrew/homebrew-bundle

# Hugo static site generator (extended version for SCSS support)
brew "hugo"

# Optional: just command runner
brew "just"
